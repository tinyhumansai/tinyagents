//! Harness runtime facade.
//!
//! Owns the high-level `AgentHarness` builder and facade that wires registries,
//! middleware, context, events, memory, limits, and the agent loop into a single
//! ergonomic entrypoint.
