//! Run configuration and runtime context.
//!
//! Owns run identity, thread identity, tags, metadata, inherited context,
//! configurable runtime values, cancellation, and dependency access passed
//! through model, tool, middleware, and graph-node calls.
